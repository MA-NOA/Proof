\documentclass[compress,t]{beamer}
\usetheme{default}

\usepackage{fontspec}
\setsansfont{Acathist}
\usecolortheme{seahorse}
\usecolortheme{rose}
\useinnertheme{circles}
\usefonttheme[onlylarge]{structuresmallcapsserif}
\usefonttheme[onlysmall]{structurebold}

\setbeamercolor{title}{fg=red!80!black,bg=red!20!white}
\setbeamertemplate{navigation symbols}{}
\setbeamertemplate{blocks}[rounded][shadow=true]
\setbeamertemplate{footline}[frame number]


\title{THERE IS NO LARGEST PRIME NUMBERS}
\author[Manoa]{IRIMANOA Nampoina Hiarijaona}
\institute{CRIAT MAdagascar \\ Center Antananarivo}
\logo{Criat Studio}
\date{\today}

\begin{document}
\begin{frame}[plain]
    \maketitle\end{frame}

\begin{frame}[plain]
	\frametitle{Outlines}
	\tableofcontents[hideallsubsections]
\end{frame}

\section{Prime numbers}
\subsection{What are prime numbers ?}
\begin{frame}{What Are Prime Number ?}
	\begin{definition}
		A prime number is a number that has exactly two divisors
	\end{definition}
	\begin{example}
		\begin{itemize}
			\item 2 is prime (two divisors: 1 and 2)
			\item 3 is prime (two divisors: 1 and 3)
			\item 4 is not prime (three divisors: 1, 2 and 4)
		\end{itemize}
	\end{example}
\end{frame}

\subsection{There is no largest prime numbers}
\begin{frame}{There Is No Largest Prime Numbers}
	\transdissolve
	\framesubtitle{The proof uses \textit{redictio ad absurdum}.}
	
	\begin{theorem}
		There is no largest prime numbers
	\end{theorem}
	
	\begin{proof}
		\begin{enumerate}
			\item Suppose $p$ is the largest prime number.
			\item Let $q$ the product of the first $p$ numbers.
			\item Then $q + 1$ is not divisile be any of them.
			\item But $q + 1$ is grreater than $1$, thus divisible by some prime numbers not in the the first $p$ prime number.\qedhere 
		\end{enumerate}
	\end{proof}
\end{frame}

\begin{frame}
	\frametitle{What Still To Do ?}
	\begin{columns}
		\column{.45\textwidth}
		\begin{block}{Answered Question}
			How many prime are there ?
		\end{block}
		
		\column{.45\textwidth}
		\begin{block}{Open Question}
			Is every even number the sum of two prime numbers ?
		\end{block}
	\end{columns}
\end{frame}

\begin{frame}[fragile]
	\frametitle{An Algorithm For Finding Prime Numbers.}
	\begin{verbatim}
int main (void)
{
  std::vector<bool> is_prime (100, true);
  for (int i = 2; i < 100; i++)
  if (is_prime[i])
  {
    std::cout << i << " ";
    for (int j = i; j < 100; is_prime [j] = false, j+=i);
  }
  return 0;
}
    \end{verbatim}
		Note the use of \alert{std::} .
\end{frame}

\end{document}
